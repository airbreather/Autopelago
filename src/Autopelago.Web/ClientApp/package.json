{
  "private": true,
  "scripts": {
    "build": "parcel build src/index.html --dist-dir ../wwwroot"
  },
  "browserslist": "> 0.001%, not dead",
  "dependencies": {
    "@microsoft/signalr": "^8.0.0"
  },
  "devDependencies": {
    "@tsconfig/recommended": "latest",
    "parcel": "latest",
    "process": "^0.11.10",
    "typescript": "latest"
  }
}
